const Messages = {
  commonExceptionMessage: 'unable to process request please try again letter.',
};

const SCREEN_NAME = {
  HOME: 'Home',
  POKEMON_DETAIL: 'Detail',
};

module.exports = {
  ...Messages,
  SCREEN_NAME,
};
